const initialState = { 
    loaderStatus:false,
    //data ebis

  };
  
  const MonitorEbisProgressReducerDlv = (state = initialState, action) => {
    switch (action.type) {


      default:
        return state;
    }
  };
  
  export default MonitorEbisProgressReducerDlv;