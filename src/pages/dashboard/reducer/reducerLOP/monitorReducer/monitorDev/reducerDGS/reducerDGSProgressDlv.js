const initialState = { 
  loaderStatus:false,

};

const MonitorDgsProgressReducerDlv = (state = initialState, action) => {
  switch (action.type) {


    default:
      return state;
  }
};

export default MonitorDgsProgressReducerDlv;