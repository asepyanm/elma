const initialState = { 
  loaderStatus:false,


};

const MonitorDbsProgressReducerDlv = (state = initialState, action) => {
  switch (action.type) {


    default:
      return state;
  }
};

export default MonitorDbsProgressReducerDlv;