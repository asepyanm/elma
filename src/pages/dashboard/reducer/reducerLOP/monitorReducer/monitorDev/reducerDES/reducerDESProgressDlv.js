const initialState = { 
  loaderStatus:false,
  //data Des

};

const MonitorDesProgressReducerDlv = (state = initialState, action) => {
  switch (action.type) {


    default:
      return state;
  }
};

export default MonitorDesProgressReducerDlv;