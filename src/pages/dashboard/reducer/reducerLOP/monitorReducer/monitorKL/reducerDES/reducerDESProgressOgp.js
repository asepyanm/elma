const initialState = { 
  loaderStatus:false,

};

const MonitorDesProgressOgpReducerKL = (state = initialState, action) => {
  switch (action.type) {


    default:
      return state;
  }
};

export default MonitorDesProgressOgpReducerKL;