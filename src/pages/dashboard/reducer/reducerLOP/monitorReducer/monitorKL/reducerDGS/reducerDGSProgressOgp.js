const initialState = { 
  loaderStatus:false,
  
};

const MonitorDgsProgressOgpReducerKL = (state = initialState, action) => {
  switch (action.type) {


    default:
      return state;
  }
};

export default MonitorDgsProgressOgpReducerKL;