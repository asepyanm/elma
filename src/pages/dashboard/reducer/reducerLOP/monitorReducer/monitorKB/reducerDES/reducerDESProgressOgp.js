const initialState = { 
  loaderStatus:false,
  //data Des
 };

const MonitorDesProgressOgpReducer = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default MonitorDesProgressOgpReducer;